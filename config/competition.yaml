# What the public scoreboard tracks. Round 1 (SCALPEL vs GPT-DAY, 2026-08-07, 30 days)
# is over; its record stays in git history and state/scalpel, state/gpt_day.
#
# Every entry is scored READ-ONLY: the scoreboard calls account() and positions()
# with the bot's own keys and never places an order. Accounts are different sizes,
# so bots are compared on % return from their own starting equity.
# `curve` keeps the scoreboard's audit copy apart from each bot's own equity file.
round: 2
title: "Bot Arena — live fleet"
start_date: "2026-09-24"
competitors:
  - {slug: talon,    label: "TALON",     kind: external, env_prefix: TALON,    starting_equity: 100000, curve: scoreboard/talon,    note: "crypto trend, 10 coins"}
  - {slug: pawl,     label: "PAWL",      kind: external, env_prefix: PAWL,     starting_equity: 1000,   curve: scoreboard/pawl,     note: "BTC/ETH trend"}
  - {slug: twincoin, label: "TWIN-COIN", kind: external, env_prefix: TWINCOIN, starting_equity: 500,    curve: scoreboard/twincoin, note: "BTC/ETH, paused"}
  - {slug: glider,   label: "GLIDER",    kind: external, env_prefix: GLIDER,   starting_equity: 5000,   curve: scoreboard/glider,   note: "stock swing"}
  - {slug: steward,  label: "STEWARD",   kind: external, env_prefix: STEWARD,  starting_equity: 5000,   curve: scoreboard/steward,  note: "ETF core"}
